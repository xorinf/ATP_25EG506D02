// 1. find the big number in givent 2 numbers
let firstNumber = 10;
let secondNumber = 20;

if (firstNumber > secondNumber) {
  console.log(`The first number : ${firstNumber} is bigger than ${secondNumber}`);
} else {
  console.log(`The second number : ${secondNumber} is bigger than ${firstNumber}`);
}