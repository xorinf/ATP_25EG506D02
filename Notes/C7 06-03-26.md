# day 7

> TCP/IP - HTTP ops \
> how javascript is async \
> eventloop used for comms (behaviour of event loop) \
> all database methods sends promise \
> new ClassName is `new` is an operator \
> use `String` which is mongoose dtype and `string` is js dtype

# Steps to create backend with DB

1. genrate package.json `npm init -y`
2. create express server
3. install mongoose and connect to mongoDB server

    Rest API -- MOngoDB native driver -> DB server
    Rest API -- Mongoose ODM tool - DB Server

4.  Build User API
    - create user
    - read all users
    - read all users by id
    - update a user by id
    - delete a user by id

5. create schema and model of the resource (User)

6. create USER API and define routes

use a findone method to read a document non object id feilds
use findbyID to read documents with object id

### STATUS CODES
- 200 : success
- 201 : created
- 400 : bad request
- 401 : unauthorized
- 404 : resource not found
- 500 : server errors