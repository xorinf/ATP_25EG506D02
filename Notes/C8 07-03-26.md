# User Authentication (submit credentials and get tokens)

    - Public routes (accessed by anyone) -> by default every route is public
    - Protected routes (by authenticated users only)

- XSS -> this is an attack used to cross site scripting attack!
- CSRF -> cross site resource forgery

request comes to api -> public api
req -> middleware? -> protected routes (by authenticated user!)

// if we use app.use it is a middleware for every route

// if you want only for specific route

- > to access cookies properties of request object we need to use (cookie parser middleware) otherwise request.cookies is undefined!
- if token is undefined then it is coming from unauthorized user



>> Task.md

-- Convert ProductAPI GET, POST, PUT, DELETE with /auth and tokenVerified!